import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sdv-hackathon-front2',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
